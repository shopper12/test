name: Legacy flight price refresh (manual only)

# 2026-08-30 final itinerary note:
# The previous hourly fare job was built for an obsolete 4-person / CPH-return plan.
# The final source document has 3 travelers and six already-ticketed flight legs,
# and does not provide a budget or fare-comparison requirement.
# Keep this workflow manual-only until a new final-itinerary fare model is defined.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retired automation
        run: |
          echo "Hourly fare refresh is retired for FINAL_0830_V1."
          echo "Use itinerary-data.js as the source of truth for ticketed flights and statuses."
